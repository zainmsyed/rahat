# Code Review 2026-07-26T00:24:42Z

**Status:** complete
**Created:** 2026-07-26T00:24:42Z
**Completed:** 2026-07-26
**Scope:** story  
**Story:** story-018  
**Static analysis:** not run (fallow unavailable)  
**Focus:** story-018 completion review  
**Trigger:** complete-story

---

## Goal
Inspect the requested scope for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete
---

## Findings

### Finding 1
- Severity: medium
- Category: bug
- Summary: `parseDay` in `cmd/server/main.go` falls back to `time.Now().UTC()` when no date is provided, which can plan the wrong local day for non-UTC users now that the scheduler interprets the passed instant's date components as the user's local date.
- Evidence: `cmd/server/main.go` `parseDay` returns `time.Now().UTC()` if the query parameter is empty. The `/schedule/plan` handler then passes that to `PlanDay`. For a user in `America/Chicago`, a request at 2026-07-26 02:00 UTC is 2026-07-25 21:00 local time; the scheduler will plan for 2026-07-25 instead of the current local day 2026-07-26.
- Recommendation: Make the `/schedule/plan` handler compute the current local date from the authenticated user's timezone when no date is provided, or make `parseDay` accept a timezone and return the local-date UTC-midnight instant.
- Rule candidate: When an endpoint accepts an optional local date, default to the current local date in the user's timezone rather than UTC now.

### Finding 2
- Severity: low
- Category: test-gap
- Summary: The new timezone-aware test only verifies the morning window.
- Evidence: `internal/scheduler/service_test.go` `TestSchedulerTimezoneAware` creates a morning task and asserts `ReadyAt` is 8:00 AM local time. It does not exercise afternoon or evening windows, which are also affected by the `daytime` package changes.
- Recommendation: Extend the test to also schedule afternoon and evening tasks and assert their local ready hours (12:00 PM and 4:00 PM respectively) across the tested timezones.
- Rule candidate: —

### Finding 3
- Severity: low
- Category: simplification
- Summary: `StartTime` and `EndTime` duplicate the same location fallback logic.
- Evidence: Both functions in `internal/time/windows.go` contain identical code: `loc := date.Location(); if loc == nil { loc = time.UTC }`.
- Recommendation: Extract a small helper such as `locationOf(date time.Time) *time.Location` and reuse it in both functions.
- Rule candidate: —

---

## Fallow Findings
- No Fallow findings.

---

## Recommended Fixes
- [x] medium — Fix `/schedule/plan` (or `parseDay`) so the empty-date fallback resolves the current local date in the user's timezone.
- [x] low — Extend `TestSchedulerTimezoneAware` to cover afternoon and evening windows.
- [x] low — Extract a shared `locationOf` helper in `internal/time/windows.go` to remove duplicated fallback logic.

---

## Other Fixes
- None.

---

## Completion Summary

All three review findings have been remediated and verified:

1. `GET /schedule/plan` now uses `localDateAsUTC(current.User.Timezone, time.Now())` when no `date` query is provided, so the empty-date fallback resolves the current local day instead of UTC now.
2. `TestSchedulerTimezoneAware` was extended to schedule morning, afternoon, and evening tasks and assert their local ready hours (8:00 AM, 12:00 PM, 4:00 PM) across `America/Chicago`, `Asia/Tokyo`, and `UTC`.
3. A shared `locationOf` helper was added in `internal/time/windows.go`, removing the duplicated location fallback logic from `StartTime` and `EndTime`.

All scheduler tests, `go test ./...`, and `npm test` pass.

No Fallow findings were pre-populated.
